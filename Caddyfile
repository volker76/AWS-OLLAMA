https://:443 {
    tls /etc/caddy/ssl/fullchain.pem /etc/caddy/ssl/privkey.pem

    @unauthorized {
        not header Authorization "Bearer SUPER_SECRET_KEY"
    }

    respond @unauthorized 401 {
        body "Unauthorized"
    }

    reverse_proxy vllm:8000
}
